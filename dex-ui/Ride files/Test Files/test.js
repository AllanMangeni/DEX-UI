const wvs = 1e8 
describe('some suite', () => {
    // before(async() => {
    //     await setupAccounts({foo: 1 * wvs, bar: 2 * wvs})
    // })
      
    it('logs something', async () => {
        console.log('foo')
    })
})